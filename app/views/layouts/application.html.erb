<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Globalhack</title>
    <%= render partial: 'layouts/common_header' %>
  </head>

  <body class="ghbase <%= controller_name + ' ' + action_name %>">
    <header>
      <%= render 'layouts/nav_bar'%>
    </header>
    <div class="container">
      <%= render 'layouts/alert_notices' %>
      <%= yield %>
    </div>
  </body>
</html>
